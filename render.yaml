services:
  - type: web
    name: echorank-backend
    env: node
    region: oregon
    buildCommand: npm ci && npm run build
    startCommand: node dist/index.js
    envVars:
      - key: NODE_ENV
        value: production